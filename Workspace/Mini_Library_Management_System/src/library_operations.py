"""
library_operations.py

Contains the core logic for library operations (add, remove, search, borrow, return books).

Interactions: Reads and writes book data using src.utils.
"""

import pandas
import datetime
from src import utils

